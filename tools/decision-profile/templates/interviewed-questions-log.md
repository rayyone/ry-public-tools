<!--
  Questions Log — interview questions already asked, so the next batch stays fresh.
  NOT read at runtime. Appended by /decision-profile interview.
  Format: - [<date>] [<domain>] (lens: <lens> | axis: <decision concept>) <question>
  lens ∈ {trade-off, means-ends, engagement, stopping, threshold, stakeholder}.
  lens + axis = what the question probed. Used for SEMANTIC dedup:
  the next batch must not re-ask any lens×axis already covered here.
-->
